= Executor API
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 2
:sectlinks:

ifndef::snippets[]
:snippets: ./build/generated-snippets
endif::[]

== Executor API

=== 사용자의 코드를 실행한다.

코드를 실행합니다. 성공하면 다음과 같은 메시지를 반환합니다.

==== HTTP request

include::{snippets}/executor/post__execute-code_return-succeeded-result/http-request.adoc[]

.Request body
include::{snippets}/executor/post__execute-code_return-succeeded-result/request-fields.adoc[]

.Request Param
include::{snippets}/executor/post__execute-code_return-succeeded-result/query-parameters.adoc[]

==== HTTP response - 실행에 성공한 경우

코드 실행에 성공하면 다음과 같은 메시지를 반환합니다. SSE 형식으로 데이터가 전달됩니다.

include::{snippets}/executor/post__execute-code_return-succeeded-result/http-response.adoc[]

==== HTTP response - 출력 값이 다른 경우

실행한 결과 값이 다르면 다음과 같은 메시지를 반환합니다.

include::{snippets}/executor/post__execute-code_return-failed-result_-cause-is-different//http-response.adoc[]

==== HTTP response - 예외가 발생한 경우

실행에 실패하면 다음과 같은 메시지를 반환합니다.

include::{snippets}/executor/post__execute-code_return-failed-result_-cause-is-error//http-response.adoc[]

> 그 외에 예외 사항은 입력 값이 없거나 문제 번호가 없으면 ``400 Bad Request``를 반환하고, 사용자 정보가 존재하지 않은 경우 ``401 UnAuthentication``을 반환합니다.

=== 결과 목록을 조회한다.

==== HTTP request

문제 식별자를 이용해 결과 목록을 조회합니다.

include::{snippets}/executor/get_find-results///http-request.adoc[]

==== HTTP response

목록을 반환합니다. 값이 존재하지 않는 경우 빈 값이 반환됩니다.

include::{snippets}/executor/get_find-results//http-response.adoc[]

> 사용자 정보가 존재하지 않은 경우 ``401 UnAuthentication``을 반환합니다.

=== 결과를 조회한다.

==== HTTP request

결과 식별자를 이용해 결과를 조회합니다.

include::{snippets}/executor/get__find-result_return-succeeded-result//http-request.adoc[]

.Request Param
include::{snippets}/executor/get__find-result_return-succeeded-result/path-parameters.adoc[]

==== HTTP response - 실행에 성공한 경우

결과를 반환합니다. 결과가 없는 경우 ``404 Not Found``를 반환합니다.

include::{snippets}/executor/get__find-result_return-succeeded-result/http-response.adoc[]

> 결과 번호가 없는 경우 ``400 Bad Request``를 반환하고, 사용자 정보가 존재하지 않은 경우 ``401 UnAuthentication``을 반환합니다.

